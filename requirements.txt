# requirements.txt

# Detectron2: Specify the version based on your Python version and CUDA version
git+https://github.com/facebookresearch/detectron2.git@main  # Replace `main` with the specific branch if needed

# Other dependencies with versions
numpy==1.24.0  # Latest stable version for numerical operations
opencv-python==4.7.0  # Latest stable version for image processing
matplotlib==3.7.1  # Latest version for visualization
scikit-image==0.21.0  # For advanced image processing functionalities
scikit-learn==1.3.0  # For additional machine learning functionalities
pillow==9.5.0  # For image processing capabilities, supports various formats
tqdm==4.65.0  # For progress bars in loops, useful for data processing tasks
